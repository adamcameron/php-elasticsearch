{% extends 'base.html.twig' %}

{% block title %}Add Student{% endblock %}

{% block body %}
    <h1>Add Student</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Add</button>
    <a href="{{ path('course_view', {id: course.id}) }}">Cancel</a>
    {{ form_end(form) }}
{% endblock %}
