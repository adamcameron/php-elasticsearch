{% extends 'base.html.twig' %}

{% block title %}Assignments by Institution, Department & Course{% endblock %}

{% block body %}
    <h1>Assignments by Institution, Department & Course</h1>
    <ul>
        {% for institution in institutions %}
            <li>
                <a href="{{ path('institution_view', {id: institution.id}) }}">
                    {{ institution.name }}
                </a>
                <ul>
                    {% for department in institution.departments %}
                        <li>
                            <a href="{{ path('department_view', {id: department.id}) }}">
                                {{ department.name }}
                            </a>
                            <ul>
                                {% for course in department.courses %}
                                    <li>
                                        <a href="{{ path('course_view', {id: course.id}) }}">
                                            {{ course.title }} ({{ course.code }})
                                        </a>
                                        <ul>
                                            {% for assignment in course.assignments %}
                                                <li>
                                                    <a href="{{ path('assignment_view', {id: assignment.id}) }}">
                                                        {{ assignment.title }}
                                                    </a>
                                                </li>
                                            {% endfor %}
                                        </ul>
                                    </li>
                                {% endfor %}
                            </ul>
                        </li>
                    {% endfor %}
                </ul>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
